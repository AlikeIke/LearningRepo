#!/bin/bash

<<exit
Exit codes tell us whether or not a command was viewed as successfully run by the bash interpreter
Exitcodes are stored in the ? variable. You can reference it with $?
An exit code of 0 means that it ran successfully, any exitcode that is not 0 denotes a failure
2 denotes failure

exit



